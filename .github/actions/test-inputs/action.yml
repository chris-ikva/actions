name: test inputs
inputs:
  org:
    type: string
secrets:
  testsecret:
    
runs:
  using: "composite"
  steps:
    - run: echo  ${{ github.event.inputs.org }}
      shell: bash

    - run: echo  ${{inputs.org}}
      shell: bash
      
    - run: echo ${{secrets.testsecret}}
